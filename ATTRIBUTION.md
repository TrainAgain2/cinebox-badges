# CineboxPRO Badges

`cinebox-gold-streaming-language.json` é uma configuração compatível com CineboxPRO.

O repositório contém a configuração de badges e preserva as URLs de imagem necessárias diretamente no ficheiro JSON.
